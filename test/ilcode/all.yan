func add(x:int, y:int) -> int {
    return x+y;
}

func main() {
    var a:int;
    a = 10;
    var b:int = 10;
    b = (a+b-10)/2*3+(a*b+add(a,b));   // expression

    if(a==10) {
        print(a+b);
    }
    if(a!=b) {
        print(a);
    }

    if(a==0 && b>0 || b==-2) {
        ;
    }

    if(!(a==0)) {
        ;
    }

    if(a>b) {
        print(a);
        while(a>0) {
            a -= 1;
            continue;
        }
    } else {
        print(b);
        while(b > 0) {
            b *= 10;
            break;
        }
    }
    var c = add(a,b);
}