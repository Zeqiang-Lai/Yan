func power(base: int, e: int) -> int {
    var i = 0;
    var result = 1;
    while(i < e) {
        result *= base;
        i += 1;
    }
    return result;
}

func main() {
    var a = 0;
    var b = 9;
    if(b == 9) {
        b = 10;
    }
    while(a < 3) {
        b += 1;
        if(5 > 3) {
            continue;
        } else {
            b = 9;
        }
        break;
    }
    print(power(5, 10));
    return 0;
}