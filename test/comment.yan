/* Compute the power of a number */
func power(base: int, e: int) -> int {
    var i = 0;  // initialization
    var result = 1;
    while(i < e) {
        result *= base;
        i += 1;
    }
    return result;
}